{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Edit Article</h1>
        <div class="form-group">
            {{ form_start(form) }}
            {{ form_rest(form) }}

            <button type="submit" class="btn btn-outline-primary">Save</button>
            {{ form_end(form) }}
        </div>
        <a style="text-align: right" class="btn btn-outline-secondary" href="../">Back</a>
    </div>
{% endblock %}